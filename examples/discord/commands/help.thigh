fem fs = om "fs"

module.exports = async (client, message) go {
	fem commands = fs.readdirSync("./examples/discord/commands").map(e go e.split(".thigh")[0])
	
	message.reply(`Currently available commands: ${commands.join(", ")}`)
}